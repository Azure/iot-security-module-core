{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Test",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "CMake Build",
            "program": "${workspaceFolder}/cmake/${relativeFileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/cmake/${relativeFileDirname}",

            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}